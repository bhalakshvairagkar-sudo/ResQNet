<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions for Sensor, Location, and Emergency Network Transmission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="com.resqnet.app.ResQNetApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ResQNet AI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResQNet"
        android:usesCleartextTraffic="true">

        <!-- Authenticated role gateway: backend determines USER / AMBULANCE / HOSPITAL. -->
        <activity
            android:name="com.resqnet.app.ui.RolePortalActivity"
            android:exported="true"
            android:theme="@style/Theme.ResQNet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- User crash-protection dashboard is reached only from the authenticated User Portal. -->
        <activity android:name="com.resqnet.app.ui.MainActivity" android:exported="false" />
        <!-- Fullscreen Crash Interception Countdown Dialog -->
        <activity
            android:name="com.resqnet.app.ui.CrashCountdownActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.ResQNet.Fullscreen" />

        <!-- Background Continuous G-Sensor & Accelerometer Detection Service -->
        <service
            android:name="com.resqnet.app.service.CrashDetectionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>
</manifest>
